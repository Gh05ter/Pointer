<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mrsj.zoombug">
    <uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/pointeter"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WorkSpace.Activity.MainActivity">

        </activity>
        <activity android:name=".WorkSpace.Activity.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER"  />
            </intent-filter>
        </activity>
        <activity android:name=".WorkSpace.Activity.IPInfoActivity" android:launchMode="singleTask"/>
<activity android:name=".WorkSpace.Activity.WebViewActivity" android:launchMode="singleTask"/>
    </application>

</manifest>